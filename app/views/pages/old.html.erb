<div class="container">
  <table class="table table-hover">
    <thead>
      <tr class="table-active">
        <th>Nombre</th>
        <th>Edad</th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tbody>
      <% @old_students.each do |old| %>
      <tr>
        <td><%= old.name %></td>
        <td><%= old.age %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
  <h1>El número de estudiantes mayores de edades es <%= @old_students.size %></h1>   
</div>
